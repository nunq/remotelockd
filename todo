readme:
// put in xsession: remotelockd > ~/remotelockd.log 2>&1

daemon:
// periodically check for net, notify if online again
// fork connection watcher, is router pingable? on fail: ping join api?

functions:
panic
netsetup
decide on a main loop
notify
